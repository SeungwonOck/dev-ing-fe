import api from "../utils/api";
import * as types from "../constants/meetUp.constants";
import { toast } from "react-toastify";
import { commonUiActions } from "./commonUiAction";

const getMeetUpList = (query) => async (dispatch) => {
    try {
        
    } catch (error) {
        
    }
};

const getMeetUpDetail = (id) => async (dispatch) => {
    try {
        
    } catch (error) {
        
    }
};

const createMeetUp = (formData, searchQuery) => async (dispatch) => {
    try {
        
    } catch (error) {
        
    }
};

const deleteMeetUp = (id, searchQuery) => async (dispatch) => {
    try {
        
    } catch (error) {
        
    }
};

const editMeetUp = (formData, id, searchQuery) => async (dispatch) => {
    try {
        
    } catch (error) {
        
    }
};

export const meetUpActions = {
  getMeetUpList,
  createMeetUp,
  deleteMeetUp,
  editMeetUp,
  getMeetUpDetail,
};
